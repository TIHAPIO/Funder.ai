import { Campaign } from '../../types/campaign'
import { generateCampaigns } from '../../utils/campaign-generator'

// Generate 50 campaigns spread across 2024
export const campaigns2024: Campaign[] = generateCampaigns()
